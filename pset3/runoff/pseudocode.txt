vote function (takes voter, rank, name
    if name equals candidate
        preference array at voter position (i) and candidate position (j) = name
        return true
    else
        return false

tabulate function
    got to rank
    for each candidate
        if not eliminated
            add up their votes

print_winner function
    half votes is voter counter divided by 2

    if candidate has more than half of the votes
        print name to stdout
        return true
    else
        return false

find_min function
    min
    for every candidate
        if not eliminated AND smaller than min
            min = candidate
            return min

is tie function (min)
    tie number
    for each candidate
        if not eliminated AND votes equals min
            tie plus one
    if tie equals candidate count
        return true
    else
        return false
        
eliminate function (min)
    for each candidate
        if candidate votes equal min
            elimated gets true