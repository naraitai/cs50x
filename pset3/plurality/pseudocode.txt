vote function (take string name)
    for every element in candidates
        if name matches
            candidate total + 1
        
    
    if name in candidates
        candidate total +1
        return true
    else
        return false


print_winner function
    for every element in candidates
        if candidate[element].votes > candidate[element + 1].votes
            temp var 1 candidate[element]
            temp var 2 candidate[element +1]
            candidate[element] = candidate[element +1]
            candidate[element + 1] = candidate[element]
            
            
            swap to candidate[element +1]
    
    
    for every element in candidates
        if candidate[element].votes is more than winner
            winner = candidate[element].names
    
    print winner
    
    find candidate or candidates with highest number of votes
    print the names of winners

DOT SYNTAX